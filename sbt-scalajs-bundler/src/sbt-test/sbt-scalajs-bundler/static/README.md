scalajs-bundler/static
=====================

An application that uses npm packages and that produces
a static HTML page.

Demonstrates how to:
- depend on npm packages ;
- differentiate prod/dev builds.
